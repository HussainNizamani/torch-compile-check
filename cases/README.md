# cases

The regression corpus: one tiny model per known bug class, each a self-contained
file following the discovery convention so it doubles as a usage example, each
carrying a known-bad torch version marker. See PLAN.md "Regression corpus".
Lands with M2; empty at M0.
