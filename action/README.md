# action

The composite GitHub Action published from this repository: it installs the
package, runs it against declared entrypoints, fails the job on the configured
`--fail-on` categories, and uploads the JSON as a build artifact. See PLAN.md
"GitHub Action". Lands with M4; empty at M0.
